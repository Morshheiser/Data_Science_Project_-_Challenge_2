
CREATE TABLE TYPE_LOAD (
    ID_TYPE_LOAD INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    LOAD_NAME VARCHAR(255) NOT NULL,
    ID_MEASUREMENT_VALUES INTEGER REFERENCES MEASUREMENT_VALUES(ID_MEASUREMENT_VALUES) ON DELETE SET NULL
);
