
CREATE TABLE PORT_COSTS (
    ID_COST INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    ID_PORT INTEGER REFERENCES PORT(ID_PORT) ON DELETE CASCADE,
    RATE_TYPE VARCHAR(255) NOT NULL,
    VALUE_PER_SHIP NUMERIC,
    VALUE_PER_TON NUMERIC
);
