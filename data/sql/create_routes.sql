
CREATE TABLE ROUTES (
    ID_ROUTE INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    ID_PORT_ORIGIN INTEGER REFERENCES PORT(ID_PORT) ON DELETE SET NULL,
    ID_PORT_DESTINATION INTEGER REFERENCES PORT(ID_PORT) ON DELETE SET NULL,
    DISTANCE NUMERIC,
    AVERAGE_TIME NUMERIC
);
