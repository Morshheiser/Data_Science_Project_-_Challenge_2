
CREATE TABLE PORT (
    ID_PORT INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
    PORT_NAME VARCHAR(255) UNIQUE NOT NULL,
    COUNTRY VARCHAR(255) NOT NULL,
    LATITUDE NUMERIC,
    LONGITUDE NUMERIC
);
